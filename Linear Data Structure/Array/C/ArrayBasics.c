#include <stdio.h>

int main() {
    // Declaring and initializing an array
    int arr[5] = {10, 20, 30, 40, 50};

    // Printing the array
    printf("Array elements:\n");
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }

    return 0;
}
